const useSafeAreaInsets = () => ({ top: 47, bottom: 34, left: 0, right: 0 });
const SafeAreaView = require('react').createElement.bind(null, 'SafeAreaView');

module.exports = {
  useSafeAreaInsets,
  SafeAreaView,
};
