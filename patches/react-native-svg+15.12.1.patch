diff --git a/node_modules/react-native-svg/lib/commonjs/web/utils/prepare.js b/node_modules/react-native-svg/lib/commonjs/web/utils/prepare.js
index d57e7c8..d15e449 100644
--- a/node_modules/react-native-svg/lib/commonjs/web/utils/prepare.js
+++ b/node_modules/react-native-svg/lib/commonjs/web/utils/prepare.js
@@ -4,7 +4,6 @@ Object.defineProperty(exports, "__esModule", {
   value: true
 });
 exports.prepare = void 0;
-var _hasProperty = require("./hasProperty");
 var _parseTransform = require("./parseTransform");
 var _resolve = require("../../lib/resolve");
 var _resolveAssetUri2 = require("../../lib/resolveAssetUri");
@@ -32,17 +31,16 @@ const prepare = (self, props = self.props) => {
     gradientTransform,
     patternTransform,
     onPress,
+    // React Native props with no DOM equivalent. `accessible` and the six
+    // responder handlers that used to be added below are handed straight to a
+    // raw <path>/<svg>, where React ignores them and logs a warning per
+    // property name — once per name, globally, so a single tappable shape
+    // anywhere fills the console. Presses on the web build are delivered by
+    // `onClick`, set at the bottom of this function.
+    accessible,
     ...rest
   } = props;
   const clean = {
-    ...((0, _hasProperty.hasTouchableProperty)(props) ? {
-      onStartShouldSetResponder: self.touchableHandleStartShouldSetResponder,
-      onResponderTerminationRequest: self.touchableHandleResponderTerminationRequest,
-      onResponderGrant: self.touchableHandleResponderGrant,
-      onResponderMove: self.touchableHandleResponderMove,
-      onResponderRelease: self.touchableHandleResponderRelease,
-      onResponderTerminate: self.touchableHandleResponderTerminate
-    } : null),
     ...rest
   };
   if (origin != null) {
diff --git a/node_modules/react-native-svg/lib/module/web/utils/prepare.js b/node_modules/react-native-svg/lib/module/web/utils/prepare.js
index 113eb2b..f69d1b1 100644
--- a/node_modules/react-native-svg/lib/module/web/utils/prepare.js
+++ b/node_modules/react-native-svg/lib/module/web/utils/prepare.js
@@ -1,4 +1,3 @@
-import { hasTouchableProperty } from './hasProperty';
 import { parseTransformProp } from './parseTransform';
 import { resolve } from '../../lib/resolve';
 import { resolveAssetUri } from '../../lib/resolveAssetUri';
@@ -26,17 +25,16 @@ export const prepare = (self, props = self.props) => {
     gradientTransform,
     patternTransform,
     onPress,
+    // React Native props with no DOM equivalent. `accessible` and the six
+    // responder handlers that used to be added below are handed straight to a
+    // raw <path>/<svg>, where React ignores them and logs a warning per
+    // property name — once per name, globally, so a single tappable shape
+    // anywhere fills the console. Presses on the web build are delivered by
+    // `onClick`, set at the bottom of this function.
+    accessible,
     ...rest
   } = props;
   const clean = {
-    ...(hasTouchableProperty(props) ? {
-      onStartShouldSetResponder: self.touchableHandleStartShouldSetResponder,
-      onResponderTerminationRequest: self.touchableHandleResponderTerminationRequest,
-      onResponderGrant: self.touchableHandleResponderGrant,
-      onResponderMove: self.touchableHandleResponderMove,
-      onResponderRelease: self.touchableHandleResponderRelease,
-      onResponderTerminate: self.touchableHandleResponderTerminate
-    } : null),
     ...rest
   };
   if (origin != null) {
diff --git a/node_modules/react-native-svg/src/web/utils/prepare.ts b/node_modules/react-native-svg/src/web/utils/prepare.ts
index 4106784..2516304 100644
--- a/node_modules/react-native-svg/src/web/utils/prepare.ts
+++ b/node_modules/react-native-svg/src/web/utils/prepare.ts
@@ -4,7 +4,6 @@ import {
 } from 'react-native';
 import { BaseProps } from '../types';
 import { WebShape } from '../WebShape';
-import { hasTouchableProperty } from './hasProperty';
 import { parseTransformProp } from './parseTransform';
 import { resolve } from '../../lib/resolve';
 import { NumberProp } from '../../lib/extract/types';
@@ -36,16 +35,17 @@ export const prepare = <T extends BaseProps>(
     gradientTransform,
     patternTransform,
     onPress,
+    // React Native props with no DOM equivalent. `accessible` and the six
+    // responder handlers below are handed straight to a raw <path>/<svg>, where
+    // React ignores them and logs a warning per property name — once per name,
+    // globally, so a single tappable shape anywhere fills the console. Presses
+    // on the web build are delivered by `onClick`, set at the bottom of this
+    // function, so nothing here is load-bearing.
+    accessible,
     ...rest
   } = props;
 
   const clean: {
-    onStartShouldSetResponder?: (e: GestureResponderEvent) => boolean;
-    onResponderMove?: (e: GestureResponderEvent) => void;
-    onResponderGrant?: (e: GestureResponderEvent) => void;
-    onResponderRelease?: (e: GestureResponderEvent) => void;
-    onResponderTerminate?: (e: GestureResponderEvent) => void;
-    onResponderTerminationRequest?: (e: GestureResponderEvent) => boolean;
     onClick?: (e: GestureResponderEvent) => void;
     transform?: string;
     gradientTransform?: string;
@@ -55,18 +55,6 @@ export const prepare = <T extends BaseProps>(
     style?: object;
     ref?: unknown;
   } = {
-    ...(hasTouchableProperty(props)
-      ? {
-          onStartShouldSetResponder:
-            self.touchableHandleStartShouldSetResponder,
-          onResponderTerminationRequest:
-            self.touchableHandleResponderTerminationRequest,
-          onResponderGrant: self.touchableHandleResponderGrant,
-          onResponderMove: self.touchableHandleResponderMove,
-          onResponderRelease: self.touchableHandleResponderRelease,
-          onResponderTerminate: self.touchableHandleResponderTerminate,
-        }
-      : null),
     ...rest,
   };
 
